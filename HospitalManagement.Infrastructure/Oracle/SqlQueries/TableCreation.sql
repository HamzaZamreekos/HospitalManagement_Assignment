--CREATE TABLE Doctors (
--    Id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    Name VARCHAR2(255) NOT NULL,
--    PhoneNumber VARCHAR2(50),
--    Salary NUMBER(10, 2) NOT NULL,
--    Address VARCHAR2(500),
--    Specialization VARCHAR2(255),
--    DateTime DATE   
--);
--CREATE TABLE Hospitals (
--    Id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    PhoneNumber VARCHAR2(20) NOT NULL,
--    Address VARCHAR2(200) NOT NULL,
--    ManagingDoctorId NUMBER NOT NULL,
--    FOREIGN KEY (ManagingDoctorId) REFERENCES Doctors(Id)
--);
--CREATE TABLE Wards (
--    Id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    Name VARCHAR2(100) NOT NULL,
--    PhoneNumber VARCHAR2(20) NOT NULL,
--    NumberOfEmployees NUMBER NOT NULL,
--    HospitalId NUMBER NOT NULL,
--    FOREIGN KEY (HospitalId) REFERENCES Hospitals(Id)
--);
--CREATE TABLE Nurses ( 
--    Id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    Name VARCHAR2(100), 
--    PhoneNumber VARCHAR2(15), 
--    Salary NUMBER(10, 2), 
--    Address VARCHAR2(255), 
--    WorkHours NUMBER 
--);
--CREATE TABLE NursesWards (
--    NurseId NUMBER NOT NULL,
--    WardId NUMBER NOT NULL,
--    PRIMARY KEY (NurseId, WardId),
--    FOREIGN KEY (NurseId) REFERENCES Nurses(Id),
--    FOREIGN KEY (WardId) REFERENCES Wards(Id)
--);
--CREATE TABLE WardDoctors (
--    WardId NUMBER NOT NULL,
--    DoctorId NUMBER NOT NULL,
--    PRIMARY KEY (WardId, DoctorId),
--    FOREIGN KEY (WardId) REFERENCES Wards(Id),
--    FOREIGN KEY (DoctorId) REFERENCES Doctors(Id)
--);
--CREATE TABLE Patients ( 
--    Id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, 
--	  Name VARCHAR2(255), 
--    Age NUMBER, 
--    Affliction VARCHAR2(255), 
--    DoctorId NUMBER, 
--    NurseId NUMBER, 
--    Discharged NUMBER(1),
--    CONSTRAINT fk_Nurse FOREIGN KEY (NurseId) REFERENCES Nurses(Id),
--    CONSTRAINT fk_Doctor FOREIGN KEY (DoctorId) REFERENCES Doctors(Id)
--);
--CREATE TABLE Operations (
--    Id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    Name VARCHAR2(255) NOT NULL,
--    OperatingDoctorId NUMBER, 
--    PatientId NUMBER, 
--    Cost NUMBER(10, 2) NOT NULL,
--    DateTime DATE,
--	FOREIGN KEY (OperatingDoctorId) REFERENCES Doctors(Id),
--	FOREIGN KEY (PatientId) REFERENCES Patients(Id)
--);
--CREATE TABLE DischargeReceipts (
--    Id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    DateTime DATE,
--    PaymentAmount DECIMAL(10, 2),
--    PatientId INT,
--    StateAtDischarge INT,
--    FOREIGN KEY (PatientId) REFERENCES Patients(Id)
--);
--CREATE TABLE Laboratories (
--    Id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    Name VARCHAR2(100) NOT NULL,
--    NumberOfRooms NUMBER NOT NULL,
--    WardId NUMBER NOT NULL,
--    FOREIGN KEY (WardId) REFERENCES Wards(Id)
--);
--CREATE TABLE Tests (
--    Id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    Name VARCHAR2(100) NOT NULL,
--    PatientId NUMBER NOT NULL,
--    LaboratoryId NUMBER NOT NULL,
--    FOREIGN KEY (PatientId) REFERENCES Patients(Id),
--    FOREIGN KEY (LaboratoryId) REFERENCES Laboratories(Id)
--);
--CREATE TABLE Devices(
--    Id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    Name VARCHAR2(100) NOT NULL,
--    LaboratoryId NUMBER NOT NULL,
--    FOREIGN KEY (LaboratoryId) REFERENCES Laboratories(Id)
--);
